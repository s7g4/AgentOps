"""AgentOps multi-agent messaging package — Version 4.

Import individual submodules directly to avoid circular imports:

    from app.messaging.message import AgentMessage, SubTask, SubTaskResult
    from app.messaging.bus import AgentBus
    from app.messaging.supervisor import SupervisorAgent
"""
